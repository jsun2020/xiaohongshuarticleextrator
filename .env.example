# Database Configuration
# For Vercel deployment with Neon PostgreSQL
DATABASE_URL=postgresql://username:password@ep-hostname.region.neon.tech/database?sslmode=require

# JWT Secret Key for authentication
JWT_SECRET=your_jwt_secret_key_here

# Next.js API URL (for development)
NEXT_PUBLIC_API_URL=http://localhost:3000

# DeepSeek API Configuration (optional, can be set via UI)
# DEEPSEEK_API_KEY=your_deepseek_api_key
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# Environment (don't change this for production)
# NODE_ENV=production